#include<stdio.h>
#include<string.h>
int main()
{
  char str1[100],str2[100];
  int i,n,j=0;
  printf("\n enter the string:");
  scanf("%s",str1);
  n=strlen(str1);
  for(i=n;i>=0;i--,j++)
  {
    str2[j]=str1[i];
  }
  printf("\n the reversed string is: %s", str2);
  return 0;
}
